import React, { Fragment } from "react"
import "./Spinner.css"

const Spinner=(props)=>{
    return(
          <Fragment>
        <div className="loader"/>
    
    </Fragment>

    )
  
}

export default Spinner
